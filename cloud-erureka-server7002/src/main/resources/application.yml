server:
  port: 7002
eureka:
  instance:
    hostname: eureka7002.com
  client:
    service-url:
      #      去掉下划线的并且小写会有问题，话得大写吗？好像不同
      defaultZone: http://eureka7001.com:7001/eureka/
    fetch-registry: false
    register-with-eureka: false
